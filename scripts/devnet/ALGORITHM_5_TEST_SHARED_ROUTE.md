# Алгоритм тестирования скрипта `5.test_shared_route_jupiter.ts`

## 📋 Общая цель
Скрипт тестирует инструкцию `shared_route` программы Flipper, которая выполняет обмен токенов через CPI-вызов к mock Jupiter программе.

---

## 🔧 Этап 1: Инициализация и загрузка конфигурации

### Строки 17-25: Загрузка конфигурации
- **Что происходит**: Читается файл `jupiter_test_config.json` с адресами созданных ранее аккаунтов
- **Кто отвечает**: Скрипт `4.setup_shared_jupiter_environment.ts` должен был создать этот конфиг
- **Что в конфиге**: vaultAuthority, mints, vaults, token accounts, platformFeeAccount

### Строки 28-41: Загрузка keypair
- **Что происходит**: Загружается приватный ключ пользователя
- **Приоритет**: Сначала `fpp-staging.json`, потом `id.json`
- **Кто это**: Пользователь, от чьего имени будет выполняться swap

### Строки 43-55: Подключение к devnet
- **Что происходит**: Создается подключение к Solana devnet, wallet, provider
- **Загружаются программы**: Flipper и Mock Jupiter через их IDL

---

## 🎯 Этап 2: Подготовка к свопу

### Строки 58-67: Загрузка адресов
**Из конфига загружаются**:
- `vaultAuthority` - PDA, который управляет vaults
- `sourceMint`, `destinationMint` - токены для обмена
- `sourceVault`, `destinationVault` - хранилища токенов Flipper
- `platformFeeAccount` - аккаунт для комиссий платформы
- Token accounts пользователя (provider)

### Строки 84-93: Параметры свопа
- **inAmount**: 100 токенов (100,000,000 с учетом 6 decimals)
- **quotedOutAmount**: 150 токенов (ожидаемый выход, курс 1:1.5)
- **slippageBps**: 50 базисных пунктов = 0.5% проскальзывания
- **platformFeeBps**: 0% комиссия для теста

---

## 💰 Этап 3: Проверка балансов ДО свопа

### Строки 95-120: Чтение балансов

**Читаются балансы пользователя**: 
- `providerSourceTokenAccount` - сколько у пользователя source токенов
- `providerDestinationTokenAccount` - сколько у пользователя destination токенов

**Читаются балансы vaults** (если они существуют):
- `sourceVault` - сколько в хранилище source токенов
- `destinationVault` - сколько в хранилище destination токенов

**Обработка ошибок**: Если vaults не существуют, показывает "N/A" вместо краша

---

## 🏊 Этап 4: Создание mock liquidity pool

### Строки 122-146: Подготовка "пула ликвидности"
- **Что создается**: Associated Token Account для destination токенов
- **Владелец**: wallet.publicKey (наш пользователь)
- **Зачем**: Mock Jupiter будет "выдавать" токены из этого пула
- **Фандинг**: Минтится `quotedOutAmount * 2` destination токенов в этот аккаунт
- **Кто отвечает**: Это имитация настоящего пула ликвидности DEX

---

## ⚡ Этап 5: Выполнение shared_route

### Строки 148-156: Маршрут обмена

```javascript
routePlan = [{
    swap: { raydium: {} },  // Тип свопа (в тесте неважно)
    percent: 100,           // Использовать 100% входных токенов
    inputIndex: 0,          // Индекс входного токена
    outputIndex: 1          // Индекс выходного токена
}]
```

### Строки 158-191: Вызов инструкции shared_route

**Кто вызывает**: Наш пользователь (wallet.payer)

#### Что происходит внутри Flipper программы:

1. **Проверяет параметры**: amount, slippage, route plan
2. **Переводит source токены**: от пользователя → в sourceVault
3. **Делает CPI к Jupiter**: вызывает mock Jupiter программу
4. **Jupiter CPI**:
   - Получает destination токены из mock liquidity pool
   - Переводит их в destinationVault Flipper
5. **Flipper переводит обратно**: из destinationVault → пользователю
6. **Применяет комиссию**: если platformFeeBps > 0

#### Accounts (кто участвует):

- `vaultAuthority` - PDA-авторизация для vaults
- `userSourceTokenAccount` - откуда берутся токены пользователя
- `userDestinationTokenAccount` - куда придут токены пользователю
- `vaultSource` - временное хранилище входных токенов
- `vaultDestination` - временное хранилище выходных токенов
- `platformFeeAccount` - куда идет комиссия платформы
- `jupiterProgram` - mock Jupiter программа для CPI

#### RemainingAccounts (дополнительные аккаунты для Jupiter):

- `[0] mockLiquidityPool` - откуда Jupiter берет токены
- `[1] wallet.publicKey` - authority для перевода

---

## 📊 Этап 6: Проверка результатов

### Строки 193-194: Ожидание подтверждения
- Пауза 3 секунды для гарантии, что транзакция обработана

### Строки 196-219: Чтение балансов ПОСЛЕ свопа
- Снова читаются все балансы (пользователя и vaults)
- Сравнивается с балансами ДО свопа

### Строки 221-242: Расчет изменений

```javascript
sourceChange = баланс_до - баланс_после  // Сколько потрачено
destChange = баланс_после - баланс_до     // Сколько получено
```

**Проверки**:
1. ✅ `sourceChange === inAmount` → source токены списались правильно
2. ✅ `destChange > 0` → destination токены получены

---

## 🎉 Этап 7: Результат

### Строки 244-248: Успешный результат
- Выводится сообщение об успешном завершении
- Показывается, что mock Jupiter правильно обработал своп

### Строки 250-257: Обработка ошибок
- Если что-то пошло не так, выводятся логи транзакции
- Помогает понять, на каком этапе произошла ошибка

---

## 🔑 Ключевые участники

1. **Пользователь (wallet.payer)** - инициатор свопа, владелец токенов
2. **Flipper программа** - координатор обмена, управляет vaults
3. **Mock Jupiter программа** - имитирует DEX, выполняет фактический обмен
4. **Vault Authority (PDA)** - имеет права на управление vaults
5. **Source/Destination Vaults** - временные хранилища токенов
6. **Mock Liquidity Pool** - источник destination токенов для свопа

---

## 📈 Ожидаемый результат

- ✅ Пользователь тратит 100 source токенов
- ✅ Пользователь получает ~150 destination токенов (минус slippage)
- ✅ Vaults остаются с балансом 0 (или минимальной rent-exempt суммой)
- ✅ Платформа получает комиссию (если platformFeeBps > 0)

---

## 🚀 Запуск теста

```bash
# Убедитесь, что setup выполнен
npm run devnet:setup-jupiter

# Создайте vaults (если еще не созданы)
npm run devnet:create-vaults

# Запустите тест
npm run devnet:test-route
```

---

## ⚠️ Возможные проблемы

### TokenAccountNotFoundError
- **Причина**: Vaults не созданы или созданы другим admin
- **Решение**: Запустите `npm run devnet:create-vaults` с id.json

### Insufficient funds
- **Причина**: У пользователя недостаточно source токенов
- **Решение**: Запустите setup снова для пополнения балансов

### UnauthorizedAdmin
- **Причина**: Пытаетесь использовать vaults, созданные другим keypair
- **Решение**: Используйте тот же keypair, что создавал vault authority
