# Mainnet Operator Management Scripts

Эти скрипты предназначены для управления операторами и Address Lookup Tables (ALT) в mainnet.

> 🚀 **Новичок?** Начните с [START_HERE.md](START_HERE.md) или [QUICK_REFERENCE.md](QUICK_REFERENCE.md)

> 📚 **Навигация:** См. [INDEX.md](INDEX.md) для быстрого поиска нужной информации

## Предварительные требования

- Убедитесь, что у вас есть keypair файл authority по пути: `~/.config/solana/fpp-staging.json`
- Убедитесь, что установлены все зависимости: `npm install`
- Убедитесь, что у вас есть достаточно SOL на балансе для транзакций

## Доступные скрипты

### 1. add_operator.ts - Добавить нового оператора

Добавляет нового оператора в adapter registry.

**Использование:**

```bash
OPERATOR_PUBKEY=<публичный_ключ_оператора> npx ts-node scripts/mainnet/add_operator.ts
```

**Пример:**

```bash
OPERATOR_PUBKEY=8cJXGoV8FCwNqbcjstCiAxdW3miy2xsBvuXSn3s64GrG npx ts-node scripts/mainnet/add_operator.ts
```

**Что делает скрипт:**
- Проверяет текущее состояние adapter registry
- Проверяет, существует ли уже оператор
- Добавляет оператора в registry
- Верифицирует успешное добавление

---

### 2. remove_operator.ts - Удалить оператора

Удаляет оператора из adapter registry.

**Использование:**

```bash
OPERATOR_PUBKEY=<публичный_ключ_оператора> npx ts-node scripts/mainnet/remove_operator.ts
```

**Пример:**

```bash
OPERATOR_PUBKEY=8cJXGoV8FCwNqbcjstCiAxdW3miy2xsBvuXSn3s64GrG npx ts-node scripts/mainnet/remove_operator.ts
```

**Что делает скрипт:**
- Проверяет текущее состояние adapter registry
- Проверяет, существует ли оператор
- Удаляет оператора из registry
- Верифицирует успешное удаление

---

### 3. replace_operator.ts - Заменить оператора

Удаляет старого оператора и добавляет нового за одну операцию.

**Использование:**

```bash
OLD_OPERATOR_PUBKEY=<старый_публичный_ключ> NEW_OPERATOR_PUBKEY=<новый_публичный_ключ> npx ts-node scripts/mainnet/replace_operator.ts
```

**Пример:**

```bash
OLD_OPERATOR_PUBKEY=8cJXGoV8FCwNqbcjstCiAxdW3miy2xsBvuXSn3s64GrG \
NEW_OPERATOR_PUBKEY=9dKLmNpvXZfGkjRt3Hq7YzLpMnUwZxEaBcRfTyGhJkWs \
npx ts-node scripts/mainnet/replace_operator.ts
```

**Что делает скрипт:**
- Проверяет текущее состояние adapter registry
- Удаляет старого оператора (если существует)
- Добавляет нового оператора
- Верифицирует успешную замену

---

### 4. transfer_alt_authority.ts - Передать права на все Address Lookup Tables

Находит все Address Lookup Tables (ALT), принадлежащие текущему authority, и передает права управления ими новому оператору.

**Использование:**

```bash
NEW_AUTHORITY_PUBKEY=<публичный_ключ_нового_authority> npx ts-node scripts/mainnet/transfer_alt_authority.ts
```

**Пример:**

```bash
NEW_AUTHORITY_PUBKEY=9dKLmNpvXZfGkjRt3Hq7YzLpMnUwZxEaBcRfTyGhJkWs npx ts-node scripts/mainnet/transfer_alt_authority.ts
```

**Что делает скрипт:**
- Ищет все Address Lookup Tables, принадлежащие текущему authority
- Для каждой таблицы проверяет текущее состояние (не заморожена ли)
- Передает права управления новому authority
- Верифицирует успешную передачу прав
- Выводит детальную сводку по всем операциям

**Важные замечания:**
- Скрипт пропускает замороженные таблицы (frozen), так как для них нельзя изменить authority
- Каждая таблица передается в отдельной транзакции
- Скрипт автоматически верифицирует все изменения после завершения
- В случае неудачи для какой-либо таблицы, скрипт продолжит работу с остальными

---

### 5. transfer_alt_authority_specific.ts - Передать права на конкретные Address Lookup Tables

Передает права управления для указанного списка Address Lookup Tables новому оператору.

**Использование:**

```bash
NEW_AUTHORITY_PUBKEY=<публичный_ключ_нового_authority> \
ALT_ADDRESSES=<адрес1,адрес2,адрес3> \
npx ts-node scripts/mainnet/transfer_alt_authority_specific.ts
```

**Пример:**

```bash
NEW_AUTHORITY_PUBKEY=9dKLmNpvXZfGkjRt3Hq7YzLpMnUwZxEaBcRfTyGhJkWs \
ALT_ADDRESSES=7YfYXkg4Tpb9jMVsrRjRLjrQ6r8BvCxTFfKqb7jMSvmE,8ZgYnWkg5Uqc0kNWtsStKmsSmjSLkmR7sLrbYcjNTwnF \
npx ts-node scripts/mainnet/transfer_alt_authority_specific.ts
```

**Что делает скрипт:**
- Принимает список конкретных адресов ALT через запятую
- Для каждой таблицы проверяет текущее состояние и authority
- Передает права управления новому authority
- Верифицирует успешную передачу прав
- Выводит детальную сводку по всем операциям

**Важные замечания:**
- Используйте этот скрипт, когда знаете конкретные адреса ALT для передачи
- Скрипт проверяет, что текущий authority совпадает с ожидаемым
- Замороженные таблицы автоматически пропускаются
- Адреса должны быть разделены запятыми без пробелов (или с пробелами после запятой)

---

### 6. list_alt.ts - Просмотр Address Lookup Tables

Отображает все Address Lookup Tables, принадлежащие текущему authority или указанному адресу.

**Использование (для текущего authority):**

```bash
npx ts-node scripts/mainnet/list_alt.ts
```

**Использование (для конкретного authority):**

```bash
TARGET_AUTHORITY=<публичный_ключ_authority> npx ts-node scripts/mainnet/list_alt.ts
```

**Пример:**

```bash
# Показать ALT для текущего authority
npx ts-node scripts/mainnet/list_alt.ts

# Показать ALT для конкретного адреса
TARGET_AUTHORITY=9dKLmNpvXZfGkjRt3Hq7YzLpMnUwZxEaBcRfTyGhJkWs npx ts-node scripts/mainnet/list_alt.ts
```

**Что делает скрипт:**
- Находит все Address Lookup Tables, принадлежащие указанному authority
- Показывает детальную информацию по каждой таблице:
  - Адрес таблицы
  - Текущий authority (или "Frozen" если заморожена)
  - Количество адресов в таблице
  - Информацию о деактивации
  - Список адресов (первые 10)
- Выводит все адреса ALT в формате, удобном для копирования (через запятую)

**Важные замечания:**
- Используйте этот скрипт перед передачей прав, чтобы узнать какие ALT принадлежат authority
- Скрипт только читает данные, не вносит никаких изменений
- Выходные данные можно использовать для `transfer_alt_authority_specific.ts`

---

## Типичные рабочие процессы

### Сценарий 1: Полная передача прав новому оператору

Если вы хотите передать все права (adapter registry операторы + все ALT) новому оператору:

```bash
# Шаг 1: Просмотрите текущие ALT
npx ts-node scripts/mainnet/list_alt.ts

# Шаг 2: Замените оператора в adapter registry
OLD_OPERATOR_PUBKEY=<старый> NEW_OPERATOR_PUBKEY=<новый> \
npx ts-node scripts/mainnet/replace_operator.ts

# Шаг 3: Передайте все ALT новому оператору
NEW_AUTHORITY_PUBKEY=<новый> \
npx ts-node scripts/mainnet/transfer_alt_authority.ts
```

### Сценарий 2: Передача только конкретных ALT

Если нужно передать только определенные Address Lookup Tables:

```bash
# Шаг 1: Просмотрите все ALT и выберите нужные
npx ts-node scripts/mainnet/list_alt.ts

# Шаг 2: Скопируйте адреса нужных ALT из вывода

# Шаг 3: Передайте выбранные ALT
NEW_AUTHORITY_PUBKEY=<новый> \
ALT_ADDRESSES=<адрес1,адрес2,адрес3> \
npx ts-node scripts/mainnet/transfer_alt_authority_specific.ts
```

### Сценарий 3: Проверка ALT другого оператора

Если нужно проверить, какие ALT принадлежат другому оператору:

```bash
TARGET_AUTHORITY=<адрес_оператора> \
npx ts-node scripts/mainnet/list_alt.ts
```

## Проверка текущих операторов

Вы можете проверить текущих операторов используя Solana Explorer или выполнив скрипт с несуществующим адресом - скрипты выводят текущее состояние перед выполнением операции.

## Безопасность

⚠️ **ВАЖНО:**
- Эти скрипты работают с mainnet
- Убедитесь, что вы понимаете, что делаете
- Проверьте адреса операторов перед запуском
- Только authority может добавлять/удалять операторов
- Храните keypair файл в безопасности

## Troubleshooting

### Ошибка: "Keypair file not found"
- Убедитесь, что файл `~/.config/solana/fpp-staging.json` существует
- Проверьте права доступа к файлу

### Ошибка: "Account does not exist"
- Убедитесь, что adapter registry инициализирован
- Запустите `initialize_adapter_registry.ts` если необходимо

### Ошибка: "InvalidAuthority"
- Убедитесь, что вы используете правильный keypair authority
- Только authority может управлять операторами

### Ошибка: "Operator already exists" или "Operator not found"
- Проверьте текущее состояние registry
- Убедитесь, что используете правильный адрес оператора

### Ошибка: "Address Lookup Table not found"
- Убедитесь, что таблица существует в сети
- Проверьте правильность адреса таблицы

### Сообщение: "Table is frozen, cannot transfer authority"
- Это нормальное поведение для замороженных таблиц
- Замороженные таблицы нельзя изменить, и их authority уже установлен навсегда
- Скрипт автоматически пропускает такие таблицы
